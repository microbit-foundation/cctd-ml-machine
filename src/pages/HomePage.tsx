import DefaultPageLayout from "../components/DefaultPageLayout";

const HomePage = () => {
  return (
    <DefaultPageLayout titleId="todo">
      <p>Hi!</p>
    </DefaultPageLayout>
  );
};

export default HomePage;
